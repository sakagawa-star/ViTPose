# セッション引き継ぎノート 2026-04-09 18:00

## 今回やったこと

### ロードマップ整理

- tmp-001（YOLO11x+案Aパイプライン）をfeat-025に統合、feat-025をClosedからOpenに戻した
- feat-027（Deep OC-SORT + HALPE 26統合）を凍結。feat-026の結果次第でスキップの可能性あり
- コミット `a52579d`

### feat-025 FR-002: YOLO11x + 案A重複除去パイプライン（実装完了・テスト済み）

- 要求仕様書にFR-002を追記、設計書にセクション4を追加
- レビューサイクル: FR-002単独で3ラウンド（高0中0に収束）、全体で2ラウンド（高0中0に収束）
- 実装完了: `scripts/run_halpe26_pipeline_yolo11.py` に案Aの重複除去を組み込み
  - `compare_dedup_methods.py` から `compute_oks_mutual`, `build_dedup_groups`, `method_a`, `HALPE26_SIGMAS` をコピー
  - bbox_thrフィルタリングを事前フィルタ方式に変更（`inference_top_down_pose_model`に`bbox_thr=None`を渡す）
  - `--oks-thr` CLI引数追加（デフォルト0.5）
  - プロファイルに`dedup`ステップ追加
- ユーザーがテスト実行し、キーポイントとスケルトン描画の結果はOK
- コミット `a744706`

## 決定事項

- **2Dキーポイント推定と人物トラッキングは構造的に統合できない**: キーポイント推定はフレーム単位、トラッキングは時系列処理。別スクリプトとして維持する
- **feat-025にtmp-001を統合**: YOLO11x+案Aパイプラインは新規案件ではなくfeat-025のFR-002として実装
- **bbox_thrフィルタ方式の統一**: `compare_dedup_methods.py`と同じ事前フィルタ方式（YOLO11x検出後にスコアフィルタ + `bbox_thr=None`で推論）を採用。`person_results`と`wb_results`のインデックス1対1対応を保証
- **feat-027凍結**: feat-026のRe-ID修正結果次第でスキップする可能性があるため

## 捨てた選択肢と理由

- **新規スクリプト作成**: `run_halpe26_pipeline_yolo11.py`の拡張の方が自然。新規スクリプトは管理コスト増
- **共通モジュールへの関数抽出**: `compare_dedup_methods.py`と`run_halpe26_pipeline_yolo11.py`の両方が独立実行可能であること優先。共通化は将来のリファクタリング対象
- **feat-027でまとめて統合**: 2Dキーポイント推定とトラッキングは構造的に統合不可。ユーザーに指摘された

## ハマりどころ

- `compare_dedup_methods.py`と既存パイプラインのbbox_thrフィルタリング方式の違い: 前者は事前フィルタ+`bbox_thr=None`、後者は`inference_top_down_pose_model`内部フィルタ。FR-002で統一が必要だった
- `run_halpe26_pipeline_yolo11.py`に案Aの重複除去ロジックがない事実を、ユーザーの質問で初めて発見した

## 学び

- `compare_dedup_methods.py`は比較ツールであり、案Aの本番パイプラインではなかった。比較と本番の用途を区別する必要がある
- 既存スクリプトの処理フローを変更する際は、フィルタリング方式の統一が重要

## 次にやること

1. **feat-025クローズ**: BACKLOG.md、CLAUDE.md等のドキュメント更新が必要（ユーザーの判断待ち）
2. **feat-026 Re-ID修正**: EMA更新前の類似度チェック追加。investigation.mdにイテレーション2として修正計画を追記 → レビュー → 実装
3. **パイプライン全体のエンドツーエンドテスト**: `run_halpe26_pipeline_yolo11.py`（案A付き）→ `postprocess_reid.py` → `visualize_tracking.py` の一連の流れを病室動画で確認

## 関連ファイル

### 変更
- `scripts/run_halpe26_pipeline_yolo11.py` — 案A重複除去ロジック追加、bbox_thr事前フィルタ化
- `docs/issues/feat-025-bb-dedup-comparison/requirements.md` — FR-002追加
- `docs/issues/feat-025-bb-dedup-comparison/design.md` — セクション4追加
- `docs/BACKLOG.md` — feat-025をOpenに戻し、feat-027を凍結

### 参照
- `scripts/compare_dedup_methods.py` — 移植元の関数（compute_oks_mutual, build_dedup_groups, method_a）
- `.claude/handovers/2026-04-09_1500.md` — 前セッション引き継ぎ
