# feat-061: YOLO 検出ゼロ時の固定 ROI フォールバック

## 概要

`run_halpe26_pipeline_yolo11.py` に、YOLO11x が `bbox_thr` 以上の person BB を
1 件も返さなかったフレームに限り、CLI で与えた固定座標 ROI を 1 個の BB として
ViTPose（WB+AIC）に流すフォールバック機構を追加する。デフォルト無効（`--fallback-roi`
を指定したときのみ有効化）。

## 背景

臥位・遮蔽（シーツで肩から上のみ可視）などの姿勢で YOLO11x（COCO 学習）が person を
検出できず、トップダウン方式のため ViTPose 推論ごとスキップされるフレームが発生する。
`scripts/diagnose_pose.py`（feat-060）による切り分けで、原因は **YOLO の検出失敗**
（`--bbox-thr 0.01` でも検出ゼロ）と確定済み。検出器の差し替え・再学習は時間的制約で
不可のため、1 人前提のドメイン特性を活かし固定 ROI を ViTPose に流すフォールバックで
当該フレームのポーズ出力を回復する。

## ステータス

- 計画中（要求仕様・設計レビュー前）

## 関連

- feat-060: 静止画 1 枚のポーズ推定診断ツール（原因切り分けに使用、YOLO 検出失敗と確定）
- feat-024: YOLO11x 検出器検証（COCO 系トップダウン検出器の限界）
- bug-005: `--device` 解決（本案件で挿入する処理は検出ループ内、device 解決は無変更）
